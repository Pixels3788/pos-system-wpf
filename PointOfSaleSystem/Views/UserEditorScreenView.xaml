<UserControl x:Class="PointOfSaleSystem.Views.UserEditorScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSaleSystem.Views"
             xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
             mc:Ignorable="d" 
             d:DesignHeight="900" 
             d:DesignWidth="1600">
    <Grid Background="#F5F5F5">

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="*"/>
                
                <RowDefinition Height="Auto"/>
                
            </Grid.RowDefinitions>

            
            <TextBlock Text="Manage Users"
                       FontSize="36"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            
            <Border Grid.Row="1"
                    Background="White"
                    CornerRadius="12"
                    Padding="20">

                <DataGrid ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">

                    <DataGrid.Columns>

                       
                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding UserId}"
                                            IsReadOnly="True"
                                            Width="*"/>

                        
                        <DataGridTextColumn Header="First Name"
                                            Binding="{Binding FirstName}"
                                            IsReadOnly="True"
                                            Width="*"/>

                        
                        <DataGridTextColumn Header="Last Name"
                                            Binding="{Binding LastName}"
                                            IsReadOnly="True"
                                            Width="*"/>

                        
                        <DataGridTextColumn Header="Email"
                                            Binding="{Binding UserEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="2*"/>

                        
                        <DataGridTextColumn Header="PIN"
                                            Binding="{Binding UserPin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*"/>

                        
                        <DataGridTextColumn Header="Role"
                                            Binding="{Binding UserRole}"
                                            IsReadOnly="True"
                                            Width="*"/>

                    </DataGrid.Columns>

                </DataGrid>
            </Border>

            
            <Grid Grid.Row="2" Margin="0,30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    
                    <ColumnDefinition Width="*"/>
                    
                </Grid.ColumnDefinitions>

                
                <StackPanel Orientation="Horizontal" Grid.Column="0" HorizontalAlignment="Left">
                    <ui:Button Content="Demote to Associate"
                               Padding="20,10"
                               Margin="0,0,15,0"
                               Command="{Binding DemoteUserCommand}"
                               CommandParameter="{Binding SelectedUser}"/>
                    <ui:Button Content="Promote to Manager"
                               Padding="20,10"
                               Margin="0,0,15,0"
                               Command="{Binding PromoteUserCommand}"
                               CommandParameter="{Binding SelectedUser}"/>
                    
                    <ui:Button Content="Save Changes"
                               Padding="20,10"
                               Command="{Binding SaveChangesCommand}"/>
                </StackPanel>

                
                <ui:Button Grid.Column="1"
                           Content="← Back to Manager Panel"
                           Padding="20,10"
                           HorizontalAlignment="Right"
                           Command="{Binding NavigateBackCommand}"/>
            </Grid>

        </Grid>
    </Grid>
</UserControl>
